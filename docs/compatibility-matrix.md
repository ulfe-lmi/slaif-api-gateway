# Compatibility Matrix

This matrix summarizes implemented behavior for reviewers. It describes the current repository state, not a future roadmap promise.

For the canonical RC2 target classification, see
[`rc2-feature-scope.md`](rc2-feature-scope.md). This document records current
implemented behavior only; a green harness on current `main` means
verification-clean for this implemented scope, not feature-full RC2.

| Feature / endpoint / field | Current support | Provider coverage | Notes | Test coverage |
| --- | --- | --- | --- | --- |
| `GET /v1/models` | Implemented | Local route/provider metadata | Returns enabled, visible routes allowed for the authenticated key; `allow_all_models=false` with empty `allowed_models` returns an empty OpenAI-shaped list | Unit, API route, integration, and mocked official-client E2E model catalog tests |
| `POST /v1/chat/completions` non-streaming | Implemented | OpenAI, OpenRouter | Full auth, policy, routing, pricing, PostgreSQL reservation, provider forwarding, accounting finalization. Billing is admission-time reservation plus post-call finalization; successful calls finalize actual usage even when actual tokens/cost exceed the pre-call reservation, and subsequent calls are blocked when finalized counters exceed limits | Unit, integration, mocked official-client E2E |
| `POST /v1/chat/completions` streaming | Implemented | OpenAI, OpenRouter | SSE streaming for accepted text deltas, local function `tool_calls` deltas, `finish_reason="tool_calls"`, logprobs chunks, and structured-output-compatible chunks. Final usage is requested; missing usage emits stream error instead of successful `[DONE]`; durable finalization recovery exists | Unit, provider adapter, integration, ASGI disconnect, mocked official-client E2E |
| Chat Completions streaming live-burn margin | Implemented | OpenAI, OpenRouter | Per-key gateway-side interruption and safe reporting for `POST /v1/chat/completions` with `stream=true`. Default policy is enabled with zero cost/token margins. Positive margins stop early, zero margins stop near the estimated boundary, and negative margins allow bounded estimated overrun. Money and token thresholds are enforced independently. The threshold-crossing chunk is withheld instead of forwarded, provider final usage/cost remains authoritative when available, and post-output client disconnect/provider error/missing-usage paths finalize as estimated interrupted usage rather than zero-cost success. PostgreSQL remains hard quota truth and the reporting source, Redis/in-memory state remains temporary operational state only, and streamed content is not stored or rendered | Policy metadata, key service, admin/CLI, stream estimator/runtime, accounting, reporting/export, and provider streaming focused tests |
| Responses streaming live-burn margin | Implemented for the supported stateless text streaming subset | OpenAI, OpenRouter | Per-key gateway-side interruption and safe estimated finalization for `POST /v1/responses` with `stream=true` in the currently supported stateless text-output slice. The monitor estimates visible `response.output_text.delta` text only, discards it after counting, withholds the threshold-crossing delta, and stops the stream early when estimated cost or token burn crosses the configured cutoff. Provider final usage remains authoritative when available; missing usage, provider error after observed output, and client disconnect after observed output finalize as estimated interrupted usage instead of zero-cost success. Unsupported generated/token-bearing typed events are not forwarded unmetered. Background/cancel, response listing, Responses audio, hosted tools, MCP/connectors, and stateful streaming remain unsupported | Policy parsing/defaults, stream estimator/runtime, accounting, provider streaming, and mocked Responses streaming route tests |
| OpenAI Completions catalog bootstrap | Implemented | OpenAI Chat Completions | `slaif-gateway bootstrap openai-completions-catalog` seeds local OpenAI provider metadata, exact `/v1/chat/completions` routes, and explicit pricing rows from a curated in-repo catalog plus an operator-controlled CSV. It does not call OpenAI, fetch pricing, create gateway keys, or store provider key values | Unit CLI/service coverage |
| Provider catalog proposal tooling | Implemented | OpenAI, OpenRouter | `slaif-gateway provider-catalog propose ...` compares official provider docs/APIs plus optional OpenAI-assisted cross-checks, then writes proposal-only route/pricing TSV, normalized JSON, warnings, and report artifacts. It never mutates provider configs, model routes, pricing rows, FX rows, keys, quotas, or usage rows. Existing route/pricing import preview, explicit confirmation, and audit remain required | Focused unit CLI/service coverage; import-validator compatibility tests |
| `POST /v1/completions` legacy | Not implemented | None | Legacy Completions route creation is rejected by the bootstrap command until endpoint forwarding, accounting, pricing, and tests are implemented | Unsupported route/error behavior only |
| `POST /v1/responses` text output | Limited | OpenAI; OpenRouter only through explicit `/v1/responses` routes | Text-output foundation with string input or bounded input item arrays, URL/data URL image input to text output, URL/inline-data file input to text output, non-streaming JSON, typed SSE streaming for stateless requests, non-streaming structured `text.format` JSON object/schema support, non-streaming local/client-side function tools, non-streaming local/client-side custom tools, non-streaming stored-response create, non-streaming `previous_response_id` for owned locally recorded provider response references, and non-streaming `conversation` for owned locally recorded provider conversation references. Input arrays support `user`, `assistant`, `system`, and `developer` message roles with string content or `input_text` content parts, user-message `input_image` URL/data URL parts behind `capabilities.responses.image_input=true`, user-message `input_file` HTTPS `file_url` or safe `filename` plus `file_data` data URL parts behind `capabilities.responses.file_input=true`, plus string-only `function_call_output` and `custom_tool_call_output` items for stateless local-tool follow-up requests. Requires explicit key endpoint permission, route/model Responses capability, provider route, and `/v1/responses` pricing. Stateless create requires `capabilities.responses.stateless=true`; `previous_response_id` instead requires `capabilities.responses.previous_response_id=true` and an active same-key response reference with matching provider/route metadata; `conversation` requires `capabilities.responses.conversations=true` and an active same-key conversation reference with matching provider metadata. `store=true` requires `stream=false` and `capabilities.responses.stored_responses=true`, then stores only safe provider response reference metadata after a successful provider response ID. JSON mode requires `capabilities.responses.json_mode=true`; JSON schema structured output requires `capabilities.responses.structured_outputs=true`; local function tools require `capabilities.responses.function_tools=true`; local custom tools require `capabilities.responses.custom_tools=true`; image input requires `capabilities.responses.image_input=true`; file input requires `capabilities.responses.file_input=true`. Streaming additionally requires explicit Responses streaming capability and route streaming support; structured streaming, function-tool streaming, custom-tool streaming, stored streaming, streaming previous-response chaining, and streaming conversations are not enabled. Injects `store=false` when omitted and default `max_output_tokens` when omitted. Rejects hosted tools, MCP/connectors, function-call/custom-tool-call input items, reasoning/stateful items, hosted-tool items, `input_image.file_id`, `input_file.file_id`, `input_audio`, image generation, file search/retrieval tools, audio output, background, conversation objects, and multimodal output. Streaming finalization uses usage from the completed response event; missing usage is not treated as zero cost | Request policy, route capability, endpoint allowlist, provider adapter/streaming, pipeline-ordering, mocked official-client E2E coverage for text, streaming, structured output, local function/custom tools, image/file input, stored create/retrieve/delete, and previous-response chaining |
| `POST /v1/responses/input_tokens` | Limited | OpenAI; OpenRouter only through explicit `/v1/responses/input_tokens` routes | Provider-reported input-token count for the already-supported stateless local Responses input subset. Requires explicit key permission for `/v1/responses/input_tokens`, a matching model route, `capabilities.responses.input_token_count=true`, and text/stateless capability. Image/file/local-tool inputs still require their existing explicit capabilities. It forwards only canonical validated fields, rejects `stream`, `store`, `max_output_tokens`, background, previous-response/conversation state, hosted tools, MCP/connectors, audio, `/v1/files`, and file IDs. It does not create a Response, reserve generation quota, or create a normal generation usage ledger row | Request policy, endpoint allowlist, route capability, canonical payload, provider adapter, and mocked official OpenAI-client E2E coverage |
| `POST /v1/responses/compact` | Limited | OpenAI; OpenRouter only through explicit `/v1/responses/compact` routes | Bounded non-streaming text-focused compaction. Requires explicit key permission for `/v1/responses/compact`, a matching model route/pricing row, and `capabilities.responses.compact=true`. Accepts `model`, required `input`, and optional `instructions`; input may be a string or message arrays with string content or `input_text`/`output_text` parts. Uses endpoint-specific quota reservation with compact output caps and finalizes from provider usage. Provider compact responses without usage fail safely instead of finalizing as zero-cost success. SLAIF does not store compact input, output, encrypted compaction content, or raw request/response bodies. Rejects streaming, store/background/conversation, compact `previous_response_id`, tools, hosted tools, media/file/audio inputs, file IDs, and unknown fields | Request policy, endpoint allowlist, route capability, canonical payload, provider adapter, quota/accounting, and mocked official-client E2E coverage |
| `GET /v1/responses/{response_id}` / `DELETE /v1/responses/{response_id}` | Limited | OpenAI; OpenRouter only through explicit native Responses routes | Ownership-checked proxying for provider-stored Responses created through SLAIF. Requires explicit key endpoint permission for retrieve/delete and an active local response-reference row owned by the authenticated gateway key. Missing, non-owned, or locally deleted references return OpenAI-shaped 404 and are not proxied. Retrieve/delete do not reserve output quota or create normal generation usage ledger rows. SLAIF stores only safe reference metadata, not prompts, completions, raw bodies, tool/media payloads, or provider secrets | Unit service/provider tests, migration/repository coverage, and mocked official-client E2E coverage |
| `GET /v1/responses/{response_id}/input_items` | Limited | OpenAI; OpenRouter only through explicit native Responses routes | Ownership-checked input-item listing for provider-stored Responses created through SLAIF. Requires explicit key endpoint permission, an active local response-reference row owned by the authenticated gateway key, compatible provider/route metadata, and `capabilities.responses.list_input_items=true` on the stored route. Supports validated `after`, `limit`, `order`, and conservative `include` query params only. Missing, non-owned, deleted, or incompatible references are not proxied. Input-item listing does not reserve output quota, create normal generation usage ledger rows, store input-item content, or inspect returned item content | Endpoint policy, provider adapter, API service, and mocked official-client E2E coverage |
| `POST /v1/conversations` / `POST`/`GET`/`DELETE /v1/conversations/{conversation_id}` | Limited | OpenAI; OpenRouter only through explicit native-compatible provider config | Ownership-checked provider conversation reference foundation. Create supports empty provider conversation creation only, persists safe conversation-reference metadata after the provider returns an ID, and rejects initial items/metadata. Update accepts metadata only, rebuilds a canonical provider payload from validated fields, and does not store or log metadata values locally. Retrieve/delete require explicit key endpoint permission and an active local conversation-reference row owned by the authenticated gateway key. Missing, non-owned, deleted, or incompatible references are not proxied. Conversation resource calls do not reserve output quota, create normal generation usage ledger rows, store conversation item content, or inspect returned item content | Endpoint policy, request validation, provider adapter, API service, migration/repository coverage |
| `POST`/`GET`/`DELETE /v1/conversations/{conversation_id}/items` | Limited | OpenAI; OpenRouter only through explicit native-compatible provider config | Ownership-checked Conversation item proxying for locally recorded provider conversations. Create supports bounded text message items only and rejects hosted/tool/media payloads. List supports validated `after`, `before`, `limit`, `order`, and conservative `include` query params; retrieve supports conservative `include`; delete proxies owned active references. Unknown, non-owned, deleted, or incompatible conversation IDs are not proxied. Item resource calls do not reserve output quota, create normal generation usage ledger rows, store item content, or inspect returned item content | Endpoint policy, request validation, provider adapter, API service, and mocked official-client E2E coverage |
| Responses cancel/list | Not implemented | None | No cancel, response listing, polling, streaming `previous_response_id`, streaming `conversation`, or compact `previous_response_id`. Non-streaming `previous_response_id`, compact, input-item listing, conversation references/update, and conversation item proxying are implemented only in their explicitly documented bounded subsets | Unsupported route/error behavior only |
| `POST /v1/audio/speech` | Implemented for bounded standalone speech subset | OpenAI; OpenRouter fails closed | Accepts validated JSON with explicit endpoint permission, route/model `audio_speech` capability, allowlisted built-in voice/format/model, bounded input/instructions, canonical provider-body reconstruction, safe binary-audio passthrough, PostgreSQL reservation/finalization, and no stored input/generated audio bytes | Unit, provider forwarding, pricing/accounting, safe-header, and mocked official-client E2E tests |
| `POST /v1/audio/transcriptions` | Implemented for bounded standalone multipart subset | OpenAI; OpenRouter fails closed | Accepts validated multipart upload plus bounded optional fields, rejects URLs/file IDs/unknown fields, reconstructs canonical multipart provider payload, reserves/finalizes quota, and does not store uploaded audio bytes or transcript text | Unit, provider forwarding, pricing/accounting, safe-header, and mocked official-client E2E tests |
| `POST /v1/audio/translations` | Implemented for bounded standalone multipart subset | OpenAI; OpenRouter fails closed | Accepts validated multipart upload plus bounded optional fields, rejects URLs/file IDs/unknown fields, reconstructs canonical multipart provider payload, reserves/finalizes quota, and does not store uploaded audio bytes or translation text | Unit, provider forwarding, pricing/accounting, safe-header, and mocked official-client E2E tests |
| `/v1/embeddings` | Not implemented | None | No embeddings forwarding/accounting path yet | Error handling only |
| Files endpoints | Not implemented | None | No `/v1/files` or `/v1/uploads` ownership/pricing/forwarding path yet | Error handling only |
| Images endpoints | Not implemented | None | No standalone image-generation/storage/forwarding path yet | Error handling only |
| Realtime audio | Not implemented | None | No transport/session/auth/quota/accounting path yet | Error handling only |
| Native Anthropic API | Not implemented | None | Anthropic-family model names may be routed through OpenRouter only | Route/OpenRouter coverage |
| `messages` | Preserved when valid | OpenAI, OpenRouter | Required and validated as a non-empty list of objects with non-empty string `role`; string/text-part content is bounded by configured caps and is not stored in ledger. User-message `image_url` content parts are accepted only when the resolved route sets `chat_image_inputs=true`; user-message inline `file` content parts are accepted only when the route sets `chat_file_inputs=true`; user-message `input_audio` content parts are accepted only when the route sets `chat_audio_inputs=true` | Request policy and forwarding tests |
| Chat Completions image input to text output | Implemented behind explicit route capability | OpenAI, OpenRouter | Supports documented user-message content parts shaped as `{type:"image_url", image_url:{url, detail?}}` with `detail` `auto`, `low`, or `high`. Remote `http`/`https` URLs and `data:image/png|jpeg|webp|gif;base64,...` data URLs are bounded by settings. SLAIF does not fetch URLs, decode/rewrite images, store/log URLs/base64 payloads, or infer exact image cost from bytes. Final accounting uses provider usage/cost once. Streaming text output, `n > 1`, function tools, non-streaming custom tools, JSON/structured outputs, and logprobs compose only when their own route capabilities allow them | Request caps, route capability, provider forwarding, streaming, accounting/no-storage, and mocked official-client E2E tests |
| Chat Completions inline file input to text output | Implemented behind explicit route capability | OpenAI, OpenRouter | Supports documented user-message content parts shaped as `{type:"file", file:{filename, file_data}}` with inline base64 data. Raw base64 is accepted by default; `data:<mime>;base64,...` is opt-in. File IDs and file URLs are rejected. SLAIF does not fetch or upload files, store/log file data, filenames, file IDs, or file URLs, or infer exact file cost from bytes. Final accounting uses provider usage/cost once. Streaming text output, `n > 1`, image input, function tools, non-streaming custom tools, JSON/structured outputs, and logprobs compose only when their own route capabilities allow them | Request caps, route capability, provider forwarding, streaming, accounting/no-storage, and mocked official-client E2E tests |
| Chat Completions audio input to text output | Implemented behind explicit route capability | OpenAI, OpenRouter | Supports documented user-message content parts shaped as `{type:"input_audio", input_audio:{data, format}}` with raw base64 `data` and `format` `wav` or `mp3`. Audio data URLs and remote audio URLs are rejected in this PR. SLAIF does not fetch URLs, transcribe locally, decode/rewrite audio, store/log audio payloads or decoded bytes, or infer exact audio cost from bytes or duration. Final accounting uses provider usage/cost once. Streaming text output, `n > 1`, image input, inline file input, function tools, non-streaming custom tools, JSON/structured outputs, and logprobs compose only when their own route capabilities allow them | Request caps, route capability, provider forwarding, streaming, accounting/no-storage, and mocked official-client E2E tests |
| `tools` / `tool_choice` | Preserved when accepted | OpenAI, OpenRouter | Local/client-side `function` tools are allowed and forwarded as ordinary JSON fields within configured count/name/description/schema caps. Route-enabled local `custom` tools are also forwarded as ordinary Chat Completions fields within custom count/name/description/format/grammar caps. Serialized object/list payloads are included in Chat Completions input/cost pre-reservation and can be rejected before provider calls when oversized | Estimator, request policy, provider no-forwarding, cost/quota, PostgreSQL integration, and route forwarding tests |
| Chat Completions `tools[].type == "custom"` | Non-streaming implemented behind explicit route capability | OpenAI, OpenRouter | Supported only as local/client-side custom tool-call intent with documented shapes: `{type:"custom", custom:{name, description?, format?}}`, where format is `text` or `grammar` with `lark`/`regex`; named custom `tool_choice` is allowed only when it references a declared custom tool. SLAIF does not execute custom tools, police downstream app behavior, or add special custom-tool billing. Streaming custom tools are rejected pending clear upstream stream-delta type support | Request policy/caps, route capability, provider forwarding, accounting, no-secret, and mocked official OpenAI-client E2E coverage |
| Hosted/provider-side Chat Completions tools | Denied by default | None | Endpoint/model allowlists do not imply hosted-tool permission. `web_search_options`, web search tool types, file search, code interpreter, computer use, image generation, tool search, MCP/connectors markers, unknown tool types, `background=true`, `external_web_access`, and search-specific models such as `gpt-5-search-api` are rejected before Redis rate limiting, routing, pricing, quota reservation, or provider forwarding | Request policy and API route tests |
| `functions` / `function_call` | Preserved when accepted | OpenAI, OpenRouter | Legacy function fields are treated like other forwarded non-message payloads and are bounded by equivalent local-function caps | Estimator and request policy tests |
| `response_format` | Preserved when accepted | OpenAI, OpenRouter | `text`, `json_object`, and bounded `json_schema` shapes are accepted; JSON schemas are included in Chat Completions input/cost pre-reservation and can be rejected before provider calls when oversized | Estimator, request policy, provider no-forwarding, cost/quota, PostgreSQL integration, and route forwarding tests |
| `stream_options.include_usage` | Forced to `true` for streaming | OpenAI, OpenRouter | Other `stream_options` keys are preserved only when the object remains within the configured cap | Unit, integration, E2E streaming tests |
| `temperature` / `top_p` / `stop` / scalar controls | Preserved when valid | OpenAI, OpenRouter | Scalar controls are type/range checked; stop sequences, user metadata, logit bias, and top-logprob controls have explicit caps and consistency checks | Request passthrough and request policy tests |
| `max_tokens` / `max_completion_tokens` | Validated/preserved or defaulted | OpenAI, OpenRouter | Conflicting values rejected; absent values inject `max_completion_tokens` | Request policy tests |
| `n` | Bounded behind explicit route capability | OpenAI, OpenRouter | Omitted or `1` is allowed unchanged. `n > 1` is accepted up to `CHAT_MAX_CHOICES_PER_REQUEST` only when the resolved route sets `chat_multiple_choices=true`. Input is estimated once, possible output reservation and estimated output cost are multiplied by `n`, provider final usage/cost is not multiplied again, one gateway request remains one usage event, and streaming chunks with multiple or interleaved choice indexes are passed through | Request policy, route policy, rate-limit/quota/pricing/accounting, provider adapter, streaming, and mocked official-client E2E tests |
| `metadata` / `user` | Preserved upstream when valid | OpenAI, OpenRouter | Metadata must be a bounded JSON object; user is byte-capped; neither is stored wholesale in usage ledger | Request passthrough tests |
| Chat Completions field registry | Implemented | OpenAI, OpenRouter | Every top-level request field is classified before forwarding as supported, gateway-mutated, local tool, hosted capability, lifecycle/state, unsupported modality, trusted-calibration-only, explicitly rejected, or unknown. Unknown top-level fields fail closed in standard and trusted-calibration modes | Request policy, route policy, forwarding, and mocked official-client E2E tests |
| Chat Completions route/model capability metadata | Implemented | OpenAI, OpenRouter | Resolved `/v1/chat/completions` routes carry explicit `capabilities.chat_completions` metadata for text chat, streaming, local function tools, local custom tools, legacy functions, JSON mode, structured outputs, logprobs, safe usage signals, hosted tool families, image input, inline file input, audio input, non-streaming audio output, broader multimodal/audio flags, service tiers, and multiple choices. Image input requires `chat_image_inputs=true`; inline file input requires `chat_file_inputs=true`; audio input requires `chat_audio_inputs=true`; audio output requires `chat_audio_outputs=true`; custom tools require `chat_custom_tools=true`; multiple choices require `chat_multiple_choices=true`; absent, false, malformed, or unknown capability metadata fails closed. The gateway checks this route/model metadata before Redis rate limiting, pricing, quota reservation, or provider forwarding. Model allowlists alone do not imply capability permission | Route capability policy, route resolution, request pipeline, bootstrap/catalog tests |
| Unknown ordinary JSON fields | Rejected | None | Unknown top-level Chat Completions fields return `unknown_chat_completion_field` before Redis rate limiting, route resolution, pricing, quota reservation, or provider forwarding | Request policy, route policy, and mocked official-client E2E tests |
| `service_tier` | Limited | OpenAI, OpenRouter | Omitted or `auto` is allowed; non-default values are rejected because local pricing is not service-tier aware | Request policy tests |
| `prediction` | Preserved when accepted | OpenAI, OpenRouter | Static-output hint must be a bounded object and is counted when forwarded | Request policy tests |
| Chat Completions audio output | Non-streaming implemented behind explicit route capability and pricing metadata | OpenAI, OpenRouter | Supports `modalities=["text","audio"]` plus top-level `audio` with allowed formats `wav`, `aac`, `mp3`, `flac`, `opus`, `pcm16` and built-in voices `alloy`, `ash`, `ballad`, `coral`, `echo`, `fable`, `nova`, `onyx`, `sage`, `shimmer`, `marin`, `cedar`. Preserves non-streaming `choices[].message.audio` response objects. Requires `chat_audio_outputs=true` and `pricing_metadata.audio_output_price_per_1m`. Streaming audio output is fail-closed for RC2 even if an unsafe config toggle is attempted; `n > 1` with audio output, custom voices, and assistant previous-audio references are also rejected | Request caps, route capability, provider forwarding, pricing/accounting, no-storage, and mocked official-client E2E tests |
| `modalities` / `audio` / broader multimodal content | Image/file/audio input plus non-streaming audio output | OpenAI, OpenRouter | `modalities=["text"]`, route-enabled `modalities=["text","audio"]`, text content parts, route-enabled user-message `image_url` parts, route-enabled inline `file` parts, and route-enabled `input_audio` parts with raw base64 `wav`/`mp3` data are allowed. Streaming audio output, audio data URLs, audio URLs, file IDs, file URLs, previous-audio references, `n > 1` with audio output, video, alternate part names such as `input_image`, and image generation remain rejected. Upstream evidence and the future implementation sequence are documented in [`chat-completions-multimodal-investigation.md`](chat-completions-multimodal-investigation.md) | Request policy tests |
| OpenAI non-streaming | Implemented | OpenAI | Uses OpenAI provider key and JSON Accept | Provider adapter tests |
| OpenAI streaming | Implemented | OpenAI | SSE parse/forward, usage chunk parse, tool-call delta and logprobs preservation, error event handling | Provider streaming tests |
| OpenRouter non-streaming | Implemented | OpenRouter | Parses usage, request IDs, and provider-reported cost metadata when present | Provider adapter tests |
| OpenRouter streaming | Implemented | OpenRouter | Parses SSE usage, cost metadata, request IDs, preserves tool-call deltas/logprobs, and handles error events safely | Provider streaming, integration, E2E tests |
| Provider errors | Implemented | OpenAI, OpenRouter | Client errors are safe; diagnostics are sanitized and bounded | Provider error and PostgreSQL diagnostic tests |
| Provider usage parsing | Implemented | OpenAI, OpenRouter | Supports `prompt_tokens`/`completion_tokens`/`total_tokens` and input/output aliases | Unit and integration accounting tests |
| Provider cost metadata | Partial | OpenRouter | Captures OpenRouter `usage.cost`/`cost_usd` as provider-reported native metadata; hard quota finalization still uses configured pricing/FX | Provider adapter and accounting tests |
| Production provider-secret validation | Implemented | OpenAI, OpenRouter | Production startup requires non-placeholder upstream secrets for enabled built-in providers, keeps `OPENAI_API_KEY` reserved for client gateway keys, and production `/readyz` checks enabled DB provider config env-var references without exposing values | Config, startup warning, provider factory, readiness, and PostgreSQL readiness tests |
| Gateway key HMAC storage | Implemented | Not provider-specific | PostgreSQL stores HMAC digest, not plaintext key | Crypto/key service tests |
| Client Authorization isolation | Implemented | OpenAI, OpenRouter | Client gateway key is replaced by provider key upstream | Provider header tests |
| Provider key isolation | Implemented | OpenAI, OpenRouter | Provider keys come from server-side env/config and are redacted from diagnostics/logs | Provider/header/redaction tests |
| PostgreSQL hard quota | Implemented | All forwarded chat requests | Reserve before forward; finalize or release after response/error | Unit and PostgreSQL integration tests |
| Redis operational rate limiting | Implemented when enabled | Chat Completions path | Request/token/concurrency throttles only; not the hard quota source of truth | Unit and Redis/PostgreSQL integration tests |
| Usage ledger without prompt/completion storage | Implemented | All accounting paths | Stores metadata, token counts, cost, status, diagnostics; not full content | Accounting, redaction, integration tests |
| Safe Chat Completions usage profiling | Implemented | OpenAI, OpenRouter | Successful finalized Chat Completions requests create advisory `usage_profiles` rows with endpoint, provider/model, sanitized provider host/path, token counts, safe tool counts/function names, and provider/SLAIF cost fields where available. Missing metrics remain null/unknown. Rows do not store prompts, completions, messages, raw bodies, full URLs, tool schemas/arguments/results, raw chain-of-thought, or secrets, and they are not invoice-grade billing truth | Unit service/model tests and PostgreSQL migration/repository/E2E tests |
| Chat Completions component cost metadata | Implemented | OpenAI, OpenRouter | Actual finalization records cost source/confidence, cached/reasoning token handling, provider-reported cost where valid, SLAIF-calculated comparison cost, and reservation-overrun flags as safe metadata. OpenRouter valid provider-reported cost is preferred for actual finalization; OpenAI uses SLAIF-calculated local pricing | Unit pricing/accounting tests, PostgreSQL overrun tests, mocked OpenRouter E2E |
| Streaming finalization recovery | Implemented | OpenAI, OpenRouter | Provider-completed finalization failures are durable and operator-repairable | Unit and PostgreSQL integration tests |
| Stale reservation reconciliation | Implemented | Not provider-specific | Operator repair for expired pending reservations; refuses provider-completed recovery rows as zero-cost stale failures. Celery/Celery Beat can be explicitly enabled for safe backlog inspection, optional generic webhook alerts, and optional auto-execution through existing reconciliation service logic; mutation is disabled by default, alerting is disabled by default, and tasks do not call providers | Unit and PostgreSQL integration tests |
| Admin web auth foundation | Implemented | Not provider-specific | `/admin/login`, `/admin/logout`, and placeholder `/admin` dashboard with server-side sessions, CSRF, DB/audit-backed failed-attempt login lockout, and explicit current role semantics: all active admins are full operators while `superadmin` is metadata/future-proofing, not enforced RBAC | Unit and PostgreSQL integration tests; optional Playwright browser smoke tests |
| Admin key pages | Implemented | Not provider-specific | `/admin/keys` list/detail pages show safe metadata only; `/admin/keys/create` and key-detail policy editing now provide selectable provider, endpoint, and route-backed model controls backed by enabled local provider configs and enabled local route metadata, with advanced manual policy strings retained as fallback. Provider allow-lists, endpoint allow-lists, and model allow-lists remain distinct, and `/v1/models` remains separate from model-backed generation endpoints. `/admin/keys/bulk-import` supports CSV/JSON bulk key creation preview and confirmed execution. Bulk preview validates owner references, optional cohort references, validity windows, hard quota fields, policy fields including optional Chat Completions streaming live-burn fields, Redis rate-limit fields, email delivery modes, upload size, row count, and secret-looking values, but does not generate plaintext keys, write `gateway_keys`, create `one_time_secrets` or `email_deliveries`, write preview audit rows, enqueue Celery tasks, send email, or call providers. Bulk execution re-parses and re-validates server-side, requires explicit confirmation, one-time plaintext display confirmation when browser plaintext will be shown, and an audit reason, then creates keys only after every row validates. Bulk execution supports `none`, `pending`, and `enqueue` email modes; bulk `send-now` is rejected before mutation. Plaintext keys are shown once on a no-cache result page for `none` and `pending` rows, are suppressed for `enqueue` rows, and are not stored. Bulk `enqueue` creates one-time secrets and pending email delivery rows, then queues Celery tasks with IDs only and does not send SMTP in the admin HTTP request. Key create/rotate support explicit `none`, `pending`, `send-now`, and `enqueue` email-delivery modes. `send-now`/`enqueue` suppress browser plaintext display and Celery payloads contain IDs only. Key detail supports CSRF-protected suspend, activate, revoke, validity-window, PostgreSQL hard quota limit, usage-counter reset, rotation, and request-policy POST actions through the existing key service and audit behavior | Unit and PostgreSQL integration tests; optional Playwright browser smoke tests |
| Admin owner/institution/cohort pages | Implemented | Not provider-specific | `/admin/owners`, `/admin/institutions`, and `/admin/cohorts` list/detail/create/edit pages show safe metadata only. Create/edit forms require CSRF and an audit reason, write safe audit rows, reject secret-looking notes/metadata, do not create keys inline, do not alter usage ledger history, and do not implement delete/anonymization workflows | Unit and PostgreSQL integration tests; optional Playwright browser smoke tests |
| Admin provider config pages | Implemented | Not provider-specific | `/admin/providers` list/detail/create/edit pages show safe local metadata and support CSRF-protected create/edit/enable/disable provider config metadata actions. Forms store `api_key_env_var` names only and never accept provider key values | Unit and PostgreSQL integration tests; optional Playwright browser smoke tests |
| Admin model route pages | Implemented | Not provider-specific | `/admin/routes` list/detail/create/edit pages show safe local routing metadata and support CSRF-protected create/edit/enable/disable model route actions. `/admin/routes/import` supports CSV/JSON/TSV preview and confirmed execution. Preview is no-mutation. Execution re-parses and re-validates server-side, requires explicit confirmation and an audit reason, writes rows only when every row is a valid supported create, rejects unknown fields, invalid provider references, invalid match types/endpoints, duplicate/conflict/update rows, and secret-looking capabilities/metadata/notes values, and does not call providers. Forms reference provider config rows and env var names only, never provider key values. Confirmed imports affect future route resolution through existing runtime semantics | Unit and PostgreSQL integration tests; optional Playwright browser smoke tests |
| Admin pricing pages | Implemented | Not provider-specific | `/admin/pricing` list/detail/create/edit pages show safe local pricing metadata and support CSRF-protected create/edit/enable/disable pricing-rule actions. `/admin/pricing/import` supports CSV/JSON/TSV preview and confirmed execution. Preview is no-mutation. Execution re-parses and re-validates server-side, requires explicit confirmation and an audit reason, writes rows only when every row is a valid supported create, parses Decimal values from strings, rejects unknown fields and secret-looking source/notes/metadata values, and does not call external pricing/provider APIs. Forms never accept provider key values | Unit and PostgreSQL integration tests; optional Playwright browser smoke tests |
| Admin FX pages | Implemented | Not provider-specific | `/admin/fx` list/detail/create/edit pages show safe local metadata and support CSRF-protected create/edit FX-rate actions. `/admin/fx/import` supports CSV/JSON preview and confirmed execution. Preview is no-mutation. Execution re-parses and re-validates server-side, requires explicit confirmation and an audit reason, writes `fx_rates` only when every row is a valid supported create, parses Decimal rates from strings, rejects unknown fields, invalid currency pairs, same-currency pairs, invalid validity windows, non-positive rates, duplicate/conflict/update rows, and secret-looking source/note/metadata values, and does not call external FX APIs or providers. Forms parse Decimal rates from strings, validate currency pairs/rates safely, and never accept provider key values. Confirmed imports affect future EUR conversion through existing runtime semantics. The schema has no enabled state, so validity windows control active status | Unit and PostgreSQL integration tests; optional Playwright browser smoke tests |
| Admin usage/audit/email delivery pages | Implemented | Not provider-specific | `/admin/usage` and `/admin/audit` pages show safe local metadata only and provide CSRF-protected CSV export controls for current filters. Exports require explicit confirmation and an audit reason, write safe export audit rows, enforce row limits, mitigate CSV formula injection, and exclude prompts, completions, raw bodies, email bodies, plaintext key material, provider keys, token hashes, encrypted payloads, password hashes, and session tokens. `/admin/email-deliveries` pages show safe delivery metadata and provide CSRF-protected send-now/enqueue actions only for pending/failed key deliveries backed by valid unconsumed one-time secrets. In-progress or ambiguous deliveries are not retried automatically; arbitrary resend actions are not exposed | Unit and PostgreSQL integration tests; optional Playwright browser smoke tests |
| Dashboard management pages | Partial | None | Key creation, bulk key import preview/execution including bulk enqueue delivery, key create/rotate email-delivery modes, email delivery send-now/enqueue for valid one-time secrets, suspend/activate/revoke, validity-window, PostgreSQL hard quota limit, usage-counter reset, rotation, owner/institution/cohort metadata create/edit actions, provider config metadata actions, model route metadata actions, route import preview/execution, pricing-rule metadata actions, pricing import preview/execution, FX-rate create/edit actions, FX import preview/execution, and usage/audit CSV exports exist; bulk key send-now execution, arbitrary old-key email resend actions, external FX refresh workflows, owner/institution/cohort delete/anonymization workflows, and usage/audit mutation pages beyond exports remain out of scope for current implementation | Admin dashboard page tests |
| Email/Celery workers | Implemented for explicit CLI workflows | Not provider-specific | Key create/rotate can create pending delivery rows, send immediately, or enqueue Celery delivery through encrypted one-time secrets; no automatic sending by default. Delivery records use `sending`/`ambiguous` state to fail closed around SMTP-accepted/database-finalization ambiguity | Unit and PostgreSQL integration tests |
| Docker Compose packaging | Implemented | Not provider-specific | Defines API, Celery worker, Celery Beat scheduler, PostgreSQL, Redis, Mailpit, and optional Nginx profile. Migrations are explicit operator actions and are not run automatically by API/worker/scheduler startup | `docker compose config` when Docker is available; docs review |
| Nginx reverse proxy example | Implemented | Not provider-specific | Proxies `/v1`, `/admin`, and `/healthz`; includes streaming-safe proxy settings; keeps `/readyz` private-network allowlisted and denies `/metrics` by default | Config/docs review |
| GitHub CI verification | Implemented | Not provider-specific | CI runs unit/lint/Alembic/diff checks, PostgreSQL integration tests, mocked OpenAI-compatible E2E tests, Playwright browser smoke, Docker Compose packaging smoke, Nginx syntax validation, documentation hygiene checks, CodeQL, and Dependabot. CI uses `TEST_DATABASE_URL` for test databases and does not require real provider keys or real email | GitHub Actions workflows |
| Beginner quickstart documentation | Implemented | Not provider-specific | `docs/quickstart.md` explains clone, `.env`, Docker Compose startup, explicit migrations, health/readiness checks, first admin, local metadata, gateway key creation, OpenAI Python client usage, Mailpit, cleanup, and troubleshooting for first-time users | Docs review |
| RC-beta release documentation | Implemented | Not provider-specific | `docs/rc-beta.md` and `docs/beta-readiness.md` document implemented scope, CI/Docker verification, known limitations, non-goals, and release checklist without claiming production certification, formal penetration testing, or compliance attestation | Docs review |
| Responses policy UI | Planned | OpenAI, OpenRouter | RC2 planned dashboard controls: default-off Responses enablement, allowed tools, model/provider caps, maximum tool calls, maximum single-request cost, and unsupported stateful/background field rejection | Future dashboard, route, service, integration, and browser tests required |
| Key templates | Implemented for calibration-derived snapshots and single-key creation | Not provider-specific | Durable `key_templates` and immutable `key_template_revisions` persist reviewed strict policy snapshots from trusted calibration proposals. Admins can create a template from the CLI or dashboard after confirmation and audit reason. Admins can create exactly one normal standard gateway key from a selected immutable revision; the key records template provenance and inherits supported policy/limits at creation time, including safe Chat Completions streaming live-burn policy metadata. Calibration-derived templates default Chat live-burn to enabled with zero cost/token margins, and older revisions without that snapshot default safely at key creation. Template revisions can carry sanitized `responses_policy` metadata for the implemented local/stored Responses subset only: text/stateless/streaming/JSON mode/structured output/function tools/custom tools/image input/file input/input-token count/stored responses/previous-response chaining/input-item listing/compact/conversations/conversation-items, local function/custom tool types, no hosted/background/storage/multimodal-output claims and no raw response IDs, conversation IDs, input items, conversation items, compact content, or payloads. Template creation and single-key creation do not mutate existing gateway keys or template revisions, do not bulk-create participants, and do not silently allow hosted capabilities | Unit service/CLI/admin tests, PostgreSQL migration/repository tests, and admin browser smoke coverage |
| Trusted calibration keys | Implemented for Chat Completions discovery | Not provider-specific | Trusted calibration keys are real gateway keys with short validity and a small request limit. They can be created from CLI and the admin key creation page. They use normal auth, route resolution, provider-secret isolation, PostgreSQL accounting, usage ledger, safe usage profiling, and audit behavior, but use `trusted_calibration_discovery` policy to observe routed Chat Completions hosted-capability needs. They are not participant keys, do not create routes automatically, and still deny unsupported endpoints, external MCP/connectors, provider-side authorization, connector IDs, server URLs, approval flows, and background/provider-state lifecycle features by default | Unit and PostgreSQL integration tests; mocked Chat Completions profile coverage; admin web unit/browser coverage |
| Calibration usage summary and strict policy proposal preview | Implemented as preview-only | Not provider-specific | Admins can summarize safe `usage_profiles` rows for trusted calibration keys from the CLI or dashboard, choose a bounded multiplier, and preview strict participant policy values. The preview shows observed endpoints/models/providers/capabilities, totals, maxima, warnings, and local-accounting cost assumptions. It does not create templates, create participant keys, mutate gateway key policies, or update routes/pricing | Unit service/CLI/admin tests, PostgreSQL usage-profile summary coverage, and admin browser smoke coverage |
| Bulk usage-derived participant-key generation | Planned | Not provider-specific | Future RC2 workflow where an admin bulk-creates participant keys from reviewed template revisions. Template persistence and single-key creation exist, but CSV/JSON bulk-from-template import and template update application are not implemented | Future bulk template-to-key service, dashboard execution, audit, integration, and browser tests required |
| Detailed Responses usage profiling | Planned | OpenAI, OpenRouter | Responses-specific profiling remains future work. Any extension must follow the current safe metadata boundary: endpoint, provider, model, sanitized provider path, token counts, safe tool-call counts, and cost fields where providers expose them; no prompt, completion, raw body, raw tool payload, raw chain-of-thought, secret, or unsafe URL storage | Future Responses provider usage parsing, redaction, accounting, export, and no-secret tests required |
| Template derivation from key usage | Implemented for trusted calibration proposals | Not provider-specific | Reviewed calibration proposals can create template revision 1 with source key, source owner display metadata, source time window, multiplier, observed summary, assumptions, warnings, and audit linkage. Hosted capabilities are review-required and are not silently participant-allowed | Unit, PostgreSQL integration, CLI/admin, and browser tests |
| Pricing catalog / price refresh | Planned | OpenAI, OpenRouter | Local pricing remains runtime source of truth. OpenRouter model metadata may seed previewed pricing proposals; OpenAI pricing remains curated/manual or confirmed import unless a stable official pricing API exists. No silent production replacement | Future pricing import/refresh, audit, and accounting tests required |
| Bounded-overrun calculator | Planned | Not provider-specific | Tool-enabled Responses policies may allow only bounded, admin-visible single-request overrun. PostgreSQL accounting must block future requests after actual overrun until limits are restored or reset | Future accounting, dashboard preview, and integration tests required |
| Real upstream calls in normal tests | Not used | None | Upstream HTTP is mocked with RESPX | Test suite configuration |
