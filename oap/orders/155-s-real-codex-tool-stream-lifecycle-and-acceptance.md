# OAP Work Order — 155-s

PR mode: AMEND_EXISTING_PR
PR: #291
Branch: oap/155-local-coding-signed-server-module
Base: main @ 7ffce834915b74809109e8b579d8541cdcfa9df7
Current remote head: 2527030f5bbb90a7f0f354eb5347caee333ce4a7

## Objective and reason

Qualify and close the exact tool-bearing Responses streaming contract required
by real Codex 0.149 through Gateway, exact Local Coding 005-m, and unchanged
protected Qwen/vLLM. Preserve immutable 155-r ordinary reasoning/message
acceptance. Add only the exact bounded function/custom-tool lifecycle and
profile composition justified by current primary sources and live evidence.

Local 005-m independently showed that a protected request carrying the accepted
Codex-derived tool envelope reached Gateway, exact Local, and protected Qwen
once, then Gateway emitted its typed stream error before terminal completion.
This is direct new evidence beyond 155-r's no-tools profile. Local/Qwen product
changes are not justified.

## Verified starting state

Gateway:

- PR #291 is OPEN, non-draft, MERGEABLE/CLEAN at immutable successful 155-r
  report head `2527030f5bbb90a7f0f354eb5347caee333ce4a7`.
- Its first parent is final implementation head
  `19d9686636b0fbf27ab96d41c610a37dad3c087a`; only
  `oap/reports/155-r-retained-event-qualification-and-final-stream.md`
  changed.
- All ten report-head checks pass and no auto-merge exists.
- 155-r's temporary hook/env/writer/sanitizer/CLI paths are absent.
- 155-r proved the exact Codex 0.149 plus resolved Local pair's ordinary
  reasoning/message stream, terminal output/usage, accounting, normal close,
  and no disconnect. That immutable result is not reopened.

Local Coding:

- PR #7 is OPEN, non-draft, MERGEABLE/CLEAN at immutable failed 005-m report
  head `4d3ab2fd97d249710f952dd3d2c28936138cc8fa`; implementation parent
  `258ae2ebad39651076937b9f027e60831b8d2786`; the report commit changes only
  `oap/reports/005-m-gateway-155r-real-codex-matrix-and-cutover-closure.md`.
- The current Local `test` check passes.
- A clean detached read-only checkout exists at
  `/home/ubuntu/codex-work/slaif-local-coding-005m` at the exact report
  head.
- 005-m support changes only acceptance harness/OAP files; no Local production
  source, dependency, or lockfile changed.

Exact 005-m evidence:

- No-live pins, official Codex 0.149.0 checksum/version, tool-policy
  differential, provider differential, full fake composition, Local tool
  filtering, identity, privacy, and cleanup gates passed.
- One protected disposable topology used exact Gateway 155-r, exact Local,
  temporary PostgreSQL, and unchanged protected Qwen.
- Ordinary non-stream text passed.
- The first tool-bearing public stream used the accepted Codex-derived envelope
  with ordinary function/custom tools plus adapter-managed disabled
  `tool_search`/`web_search`; Local removed hosted-search declarations and
  preserved ordinary tools.
- That stream returned 2xx SSE, one created event, one Local request, one
  protected provider call, normal transport close, and a Gateway error before
  completion. Reservation was terminal; ledger was not terminal.
- No retry or alternate protected stream ran.
- The actual real Codex governance/tool process was NOT RUN because the
  synthetic public stream gate failed first. Do not misstate 005-m as proof of
  a completed real-Codex process.
- Protected Qwen and all host state remained unchanged; exact cleanup passed.

Abort before protected traffic if either exact head/topology/check state,
order bytes, dependency cleanliness, runtime health/model, official Codex
0.149 identity, or resource preconditions differ.

## 1. Exact-head and real-Codex tool-envelope preflight

Before Docker, listeners, PostgreSQL, Local, or protected inference:

1. Pin verifier topology to exact 155-s activation, immutable Gateway 155-r
   report/implementation, and immutable Local 005-m report/implementation.
2. Use only the detached Local 005-m checkout read-only. Never edit, commit,
   push, or install into that worktree.
3. Reuse the official Codex 0.149.0 installation/capture contract and verify its
   literal version/checksum in a private task root.
4. Capture a fresh no-provider real Codex tool-bearing envelope from a
   synthetic workspace. Retain only safe tool type/count/relationship classes,
   never schemas, descriptions, arguments, IDs, prompts, or values.
5. Prove Gateway request policy admits only the exact Codex/Local pair and
   adapter-managed disabled-search declarations; Local filtering removes
   `tool_search`/`web_search` before provider work while preserving declared
   ordinary function/custom tools.
6. Prove hosted search choice, undeclared tools, non-Local routes, missing
   capabilities, unknown tool types, and authority-bearing extras fail before
   provider/reservation.
7. Reproduce the current validator/profile branches with pure fake events.
   Specifically prove ordinary 155-r reasoning/message remains green;
   reasoning-only profile denies tool events; tool-bearing exact pair carries
   a bounded declared-tool set; and no combination grants hosted-tool or
   unknown-event authority.

No protected request is allowed until these pure/static gates pass.

## 2. Temporary bounded first-rejection evidence

If pure evidence cannot identify the exact rejected live event, add one
temporary opt-in hook at the existing validator rejection boundary. It may
record only the first rejected event's:

- strict bounded event type or `other`;
- sorted bounded top-level field names and JSON type classes;
- sorted bounded immediate nested-object field names/type classes;
- finite validator profile booleans and declared-tool count class;
- fixed rejection outcome/code.

Never record values, deltas, text, reasoning, IDs, tool names/arguments/results,
schemas, descriptions, usage values, bodies, headers, credentials, signatures,
sessions, endpoints, paths, errors, or arbitrary exceptions. Enforce exact
mode-0700 owner-validated root, mode-0600 atomic write-once artifact,
no-follow/no-overwrite, bounded names/count/nesting/bytes, and fail-closed
canonical sanitization. Pure tests must prove private-marker exclusion,
malformed/tampered no-output, and disabled production behavior.

The hook is temporary and must be removed before final acceptance.

## 3. One real-Codex protected qualification

After the complete preflight and all ten checks pass on the temporary
qualification head, run exactly one no-retry real Codex 0.149 tool-bearing
qualification from a private synthetic workspace:

    real Codex 0.149
    -> disposable exact Gateway PR #291
    -> exact Local 005-m
    -> unchanged protected Qwen/vLLM

Use a deterministic synthetic instruction requiring one ordinary local
function/custom tool action. Use the actual captured Codex envelope, not a
handwritten substitute or official-client-only request. Do not use images,
customer/governance data, hosted search, or a direct-provider diagnostic.

Require the initial public Responses request to reach Local once and protected
Qwen once before any rejection. Stop on the first Gateway validator rejection.
Retain only the safe rejection record, finite profile/tool relationship facts,
request/call count classes, accounting terminal classes, and disconnect class.
Do not retry, change the prompt, or substitute a synthetic stream.

If no rejection occurs and the complete real Codex tool roundtrip passes all
section 7 predicates, remove the hook and use this as final evidence; do not
send a redundant final run.

## 4. Primary-source legitimacy decision

Compare the exact safe rejected shape/lifecycle against:

1. pinned official OpenAI Python 2.41.0 Responses tool event types, tag/commit
   `v2.41.0` / `2d955a1ac69df0288b8072bbcd25905639e9b2ed`;
2. protected vLLM 0.27.1 exact Responses streaming source, tag/commit
   `v0.27.1` / `6e448d0ea9bf3d88d898b65449ca6dc2aec170ac`;
3. actual Codex 0.149 captured tool envelope and exact
   `codex-0.149-responses-v1 -> local-coding-v1` contract;
4. Local 005-m filter evidence showing only ordinary declared tools reach Qwen.

No event is legitimate by name resemblance alone.

If malformed, provider-specific outside the reviewed Responses contract,
undeclared, authority-widening, or unsupported, make no Gateway relaxation.
Remove the hook, publish the bounded external defect, and do not send a final
protected request.

If legitimate, implement only its exact declared function/custom-tool
lifecycle and necessary profile composition. The pair-local ordinary
reasoning/message validator must continue to handle non-tool events; the
tool-bearing gate may delegate only exact declared tool items/deltas/done
events to the existing bounded tool validator. Hosted search and unknown
events remain denied.

## 5. Required permanent validation and negatives

For each accepted tool lifecycle, require as applicable:

- exact event field set and event type;
- bounded item ID, call ID, namespace, tool name, argument/input delta, and
  cumulative bytes;
- exact match to one declared client tool after Local/Gateway normalization;
- unique item/call IDs and bounded active item cardinality;
- added/in-progress before deltas; no orphan delta/done;
- monotonically increasing sequence and stable output index;
- delta accumulation equals terminal arguments/input;
- exactly one terminal item.done and no delta/event after done;
- terminal response.completed only after all active reasoning/message/tool
  items close;
- completed output/usage/accounting relations remain valid.

Negative tests must cover missing/extra fields, wrong types/statuses/indices,
undeclared namespace/name/type, duplicate item/call IDs, reordered/orphan
events, mismatched terminal text, overflow, excessive active items, message or
hosted-search smuggling, unknown event types, and tool events when the request
did not activate the exact tool gate.

Preserve the non-tool 155-r path and unrelated provider/client routes exactly.

## 6. Remove diagnostics and prove hook-free final head

Before final protected verification:

- remove every 155-s hook, environment variable, writer, artifact/sanitizer,
  qualification-only CLI/path, and temporary test;
- retain only the exact pair-local profile composition, tool lifecycle
  correction, permanent negatives, real-Codex verifier, and truthful docs;
- prove no production/test tree can emit or persist rejected-event shapes;
- run focused validator/request-policy/identity/privacy/accounting/E2E tests,
  full Ruff/compilation/diff checks, and exact Local/Gateway shared vectors;
- run a hook-free fake tool-call stream/roundtrip with realistic OpenAI/vLLM
  function/custom lifecycle, terminal usage/accounting, no hosted tools, no
  rejection, normal close, and cleanup;
- push the final implementation and require all ten checks green.

Fake success is rehearsal only.

## 7. One decisive real-Codex tool-bearing protected acceptance

On the unchanged hook-free green implementation head, privately revalidate
protected health/model and run exactly one no-retry real Codex 0.149
tool-bearing composed session through exact Local 005-m and unchanged Qwen.

Require:

- the real Codex request contains the accepted bounded tool envelope;
- exact Codex/Local route admission and signed identity;
- each Codex Responses turn reaches Local exactly once and Qwen exactly once;
- one intended ordinary tool call is emitted, accepted, executed locally by
  Codex, and returned in the bounded roundtrip;
- hosted `tool_search`/`web_search` does not reach Qwen and no provider-
  hosted tool authority exists;
- legitimate function/custom tool-call added/delta/done lifecycle passes;
- no `responses_stream_event_not_supported`, Gateway error, duplicate
  provider effect, or Gateway-induced Local disconnect;
- each required response lifecycle has one valid terminal
  `response.completed`; the final Codex process exits successfully;
- valid detailed usage, finalized reservations/ledgers, zero pending/corrupt
  accounting, and expected request/provider call counts for exactly one tool
  roundtrip;
- normal upstream/downstream close and complete cleanup;
- malformed/reordered/duplicate/smuggled/unbounded/unauthorized variants remain
  proven fail-closed by permanent tests.

Retain only fixed states, booleans, bounded count classes, versions/commits,
and timing buckets. Never retain prompts, model/tool text, tool arguments or
results, IDs, identities, raw SSE, or private endpoint/credential facts.

## Allowed paths

    app/slaif_gateway/providers/streaming.py
    app/slaif_gateway/services/responses_gateway.py
    tests/unit/test_responses_codex_streaming_tools.py
    tests/unit/test_v1_responses_quota.py
    tests/unit/test_responses_streaming_live_burn.py
    tests/e2e/test_openai_python_client_responses.py
    scripts/verify_local_coding_full_stack.py
    scripts/capture_codex_protocol.py
    tests/unit/test_local_coding_full_stack_verifier.py
    docs/provider-forwarding-contract.md
    docs/responses-compatibility.md
    docs/compatibility-matrix.md
    docs/accounting.md
    oap/active
    oap/orders/155-s-real-codex-tool-stream-lifecycle-and-acceptance.md
    oap/reports/155-s-real-codex-tool-stream-lifecycle-and-acceptance.md

No Local Coding or Qwen mutation, dependency/lockfile/schema/migration,
route-pair/request-envelope redesign, Compose/deployment, release, cutover, or
unrelated Gateway feature is authorized.

## Security, accounting, cleanup, and publication

Preserve provider credential secrecy, signed identity, replay/idempotency,
quota reservation/finalization, PostgreSQL accounting authority, Local route
containment, hosted-search denial, incremental cancellation semantics, and no
default content/reasoning/tool retention.

Use unique mode-0700 task roots and mode-0600 private references. Never print,
commit, hash, or persist runtime/credential contents. Permanently delete exact
task roots and secret copies without moving them to Trash. Preserve both
detached Local checkouts until the immutable report is published.

Publish exactly one immutable
`oap/reports/155-s-real-codex-tool-stream-lifecycle-and-acceptance.md` with
literal final implementation head, `Report publication commit: SELF`,
report-only topology, exact safe rejected shape/profile, primary-source
decision, correction/negative-test ledger, qualification/final real-Codex run
counts, terminal/tool/accounting/disconnect facts, checks, privacy, cleanup,
and limitations.

After publication make no repository mutation. Wait for all ten report-head
checks; verify topology/mergeability/clean state/resource absence; send exact
two bytes `OK` on the response FIFO and stop.

A passing report must be posted directly to Local PR #7 for prepared 005-n.
Neither PR merges in this round. No Objective 156, release, cutover,
certification, or MVP-complete claim is authorized by 155-s alone.
