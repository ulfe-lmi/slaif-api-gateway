# OAP Work Order — 155-z

PR mode: AMEND_EXISTING_PR
PR: #291
Branch: oap/155-local-coding-signed-server-module
Base: main @ 7ffce834915b74809109e8b579d8541cdcfa9df7
Current remote head: b499323eacbd450f566df0a6ad768a9437dff025

## Objective and reason

Retain the exact privacy-safe Gateway error contract for the real second Codex
0.149 request, correct only that proven adjacent function-pair rejection, remove all
temporary qualification diagnostics, and execute the decisive hook-free protected
two-turn acceptance.

155-y is immutable and terminally failed. It disproved the standalone-output
hypothesis and reverted every speculative product change. Its one completed protected
qualification again proved Gateway 2 requests (2xx then 4xx), Local 1 request (2xx),
protected Qwen 1 inference (2xx normal close), no disconnect or stream rejection
artifact, and one finalized accounting row with zero pending. Crucially, its ordered
safe projection proves the second request is
`top_level_function_pair_without_additional_tools`: an adjacent `function_call` then
`function_call_output`, a shape already intended by the exact 0.149 contract.

The remaining blocker is not the resolved first-turn SSE item shape and not a
standalone continuation. It is the unknown Gateway 4xx contract on the recognized
second-turn pair. The y verifier checked generic g2/l1/q1 counts before retaining the
already-captured Gateway error code/parameter class. This final continuation must fix
that evidence precedence first, diagnose once, and then change product behavior only
from the retained class.

## Verified starting state

- PR #291 is OPEN, non-draft, MERGEABLE/CLEAN, no auto-merge, at immutable
  report-only head `b499323eacbd450f566df0a6ad768a9437dff025`.
- Its first parent is cleanup implementation head
  `70a5224ff9e7dd07bf2d957baf4cb6717a39e896`; the report commit changes only
  `oap/reports/155-y-second-turn-continuation-admission-and-final-closure.md`.
- All ten report-head checks pass.
- Relative to activation `b860216cc6218920ffc5cf086359f582de8176d0`, the cleanup
  implementation changes only the verifier and its focused tests to retain ordered
  `request_profile_classes`; replay/policy/gateway production behavior and product
  tests are byte-identical to activation.
- Local Coding PR #7 remains immutable/green at report head
  `4d3ab2fd97d249710f952dd3d2c28936138cc8fa`, with read-only checkout
  `/home/ubuntu/codex-work/slaif-local-coding-005m`.
- 155-r through 155-y and all earlier reports are immutable. Do not rewrite them.
- The private runtime reference remains a mode-0600 owner-only regular file; never
  render, log, hash, commit, or retain its endpoint/credential values.
- This is the final legal continuation letter for Objective 155. Do not invent
  `155-aa` or advance to another numeric objective while PR #291 remains unresolved.

## 1. Fix failure classification before protected traffic

Before any protected request, change only the verifier/fault-injection evidence path
so a second Gateway 4xx with g2/l1/q1 is classified before the generic turn-count
mismatch. The safe retained result must include:

- ordered request profile classes for at most two requests;
- Gateway/Local/Qwen request/response/status/content-type/count classes;
- second Gateway error-code class;
- error parameter root class and allowlisted leaf-field class;
- bounded ordered second-input item types;
- for the adjacent function call/output only, sorted field-name/coarse-type sets;
- bounded top-level tool-type counts;
- accounting state/count classes and zero-pending boolean.

Allowlisted parameter leaf classes may identify only protocol field names such as
`type`, `id`, `status`, `name`, `namespace`, `arguments`, `call_id`, `output`,
`input`, `tools`, `tool_choice`, or `stream`; never retain an index, ID value, name
value, argument/result, prompt, body, header, endpoint, credential, arbitrary string,
or exception text. Unknown values map to `other`.

Add pure/fake matrices proving exact precedence for at least request-policy invalid,
replay-reference missing, replay-route mismatch, route-capability denial, and unknown
Gateway errors. Prove a g2/l1/q1 fake produces the specific allowlisted error class,
not the generic count code. Preserve provider-failure, forced-validator-rejection,
valid fake, ordinary stream, first-turn function SSE, privacy, and accounting gates.

If the likely error is replay-related, install before the diagnostic a temporary
exact-pair, mode-0600, write-once safe decision artifact that records only booleans or
bounded count classes for: request replay-candidate kinds/count, persisted lookup row
count, item-HMAC match, call-HMAC match, HMAC-version match, finalized-source-ledger
check, stored/request function-kind match, taxonomy namespace/name match, route match,
and same derived signed-session scope. No raw identifier, digest, namespace/name,
session, key, route UUID, or model may be recorded. Fault-inject and privacy-test this
artifact. If the error is policy/capability-related, retain only the field/type and
safe code/parameter evidence above; do not add irrelevant replay instrumentation.

Run full Ruff, compilation, focused verifier/policy/replay/route/accounting/privacy
tests, all three direct fake qualification modes, push the diagnostic head, and
require all ten checks green. No protected request occurs before these gates.

## 2. One bounded pre-correction protected diagnostic

On the exact clean green diagnostic head, run exactly one zero-retry real Codex 0.149
-> Gateway -> exact Local -> unchanged protected Qwen diagnostic. Direct stdout only;
no redirect, pipe, command substitution, or private task-file retention.

The expected bounded topology is Gateway 2 / Local 1 / Qwen 1. Retain the exact safe
second-request error/parameter/profile/field-type class and, when applicable, the
safe replay-decision booleans/counts. If it unexpectedly completes the full two-turn
path with correct accounting, treat that only as qualification evidence and continue
through mandatory diagnostic removal and the hook-free final; do not add a product
fix without a reproduced defect.

If the diagnostic remains `other`, malformed, missing, privacy-unsafe, or internally
inconsistent, stop with a truthful immutable FAILED report. Do not guess and do not
send another diagnostic. If it returns a recognized external-state defect, reproduce
that boundary safely and report it; do not conceal it in Gateway.

## 3. Correct only the proven Gateway branch

For a recognized Gateway-owned error, reproduce the exact branch with pure/fake and
database-backed tests before changing product code. Then implement the minimum exact
`codex-0.149-responses-v1` -> `local-coding-v1` correction.

If replay ownership is the defect, do not bypass verification. Bind the second pair
to exactly one finalized, unexpired first-turn function-call reference owned by the
same authenticated Gateway key, derived signed coding session, provider, route,
upstream model, function kind, approved taxonomy namespace/name, item/call identity,
and active HMAC version. Use constant-time digest comparison, reject zero/multiple
matches, and store no raw IDs/session. A caller-controlled ID never creates
authority. No schema/migration change is authorized unless the existing HMAC columns
cannot represent the proved contract; in that case stop rather than improvise.

If request policy or route capability is the defect, accept only the exact legitimate
field/shape proved by pinned Codex 0.149 source/types and the live safe projection,
and only for the exact Local pair. Do not globally allow unknown fields/events or
change generic/0.147/non-Local behavior.

Required negative coverage includes malformed/unknown fields; reordered, missing,
duplicate, mixed or unbounded call/output items; item/call ID mismatch; function/
custom substitution; undeclared or ambiguous taxonomy; smuggled `additional_tools`,
hosted search, MCP, namespace or tool authority; cross-key/user/session/route/model
replay; expired/unfinalized/zero/multiple references; HMAC-version mismatch; and
failure before reservation/provider side effects where the existing contract
requires it. Preserve quota reservation/finalization, accounting ownership,
idempotency, signed identity, route containment, no content storage, and normal
generic replay ordering.

Do not modify Local Coding, Qwen, the resolved first-turn SSE validator, hosted-tool
authority, dependencies/locks, schema/migrations, unrelated Gateway behavior, or
documentation beyond the exact compatibility/accounting contract if behavior changes.

## 4. Mandatory temporary-diagnostic removal and final-head gates

After the correction passes pure/fake evidence and before final protected traffic:

- remove every temporary 155-v through 155-z production hook/env/writer, rejection/
  decision/summary artifact, forced failure mode, qualification-only CLI/mode/stage,
  temporary path/symbol, and diagnostic-only test;
- preserve the permanent correction, strict negatives, zero-retry capture config,
  ordinary fake verifier, and permanent hook-free `--tool-roundtrip-protected`;
- prove hook/qualification/artifact/summary symbol absence with AST/source/search
  scoped to product/scripts/tests (historical immutable OAP files are excluded);
- run the permanent fake two-turn path, all affected tests, full Ruff, compilation,
  diff/scope checks, push the hook-free implementation head, and require all ten PR
  checks green;
- revalidate exact Local head/cleanliness, runtime permissions/shape, protected model
  health, clean Gateway worktree, no task roots/listeners/processes/containers, and
  zero pending test accounting. No protected traffic occurs during cleanup/CI.

## 5. One decisive hook-free protected final

On the exact clean hook-free green head, execute exactly one zero-retry permanent
`--tool-roundtrip-protected` run:

real Codex 0.149 -> real Gateway -> exact Local Coding -> unchanged protected Qwen
-> streamed answer through Local and Gateway to Codex.

Acceptance requires:

- Gateway requests/responses exactly 2, both successful SSE, no Gateway 4xx/error;
- Local requests/responses exactly 2, both successful SSE;
- Qwen inferences exactly 2 for the intended two-turn topology, both 2xx with normal
  close;
- first function-call lifecycle accepted, exactly one client function execution,
  second adjacent function-call/output continuation authorized and forwarded once;
- final assistant/message lifecycle and terminal completion valid, with expected
  created/completed cardinality and detailed usage per turn;
- two finalized reservations/ledger rows as required, coherent aggregate usage,
  zero failed/estimated/pending residue;
- signed identity accepted, same-session reuse, independent-session isolation,
  replay/idempotency/tamper/route ownership correct, hosted search absent, no secret/
  content/internal-header retention, no Gateway-induced Local disconnect;
- complete task/database/process/listener/container cleanup and protected post-health.

Do not retry a failed final. Publish its exact permanent bounded failure class and no
broader claim.

## Allowed paths

    app/slaif_gateway/modules/clients/codex_0149.py
    app/slaif_gateway/providers/streaming.py
    app/slaif_gateway/services/responses_gateway.py
    app/slaif_gateway/services/responses_request_policy.py
    app/slaif_gateway/services/codex_replay_service.py
    app/slaif_gateway/db/repositories/codex_replay.py
    scripts/capture_codex_protocol.py
    scripts/verify_local_coding_full_stack.py
    tests/unit/test_responses_codex_streaming_tools.py
    tests/unit/test_responses_codex_multiturn_replay.py
    tests/unit/test_codex_replay_service.py
    tests/unit/test_v1_responses_quota.py
    tests/unit/test_upstream_payload_reconstruction.py
    tests/unit/test_local_coding_full_stack_verifier.py
    tests/e2e/test_openai_python_client_responses.py
    docs/provider-forwarding-contract.md
    docs/responses-compatibility.md
    docs/compatibility-matrix.md
    docs/accounting.md
    oap/active
    oap/orders/155-z-exact-second-request-error-and-decisive-closure.md
    oap/reports/155-z-exact-second-request-error-and-decisive-closure.md

No Local/Qwen mutation, generalized Responses relaxation, hosted/custom/MCP authority
expansion, unrelated cleanup, merge, auto-merge, release, Objective 156/006, or
post-z Objective 155 continuation is authorized.

## Report and cross-repository handoff

Publish one immutable report-only commit. `RESULT=PASSED` requires the decisive
hook-free protected final; otherwise `RESULT=FAILED`. Record activation,
diagnostic/correction/cleanup/final/report topology; preserved report chain; exact
safe error/decision evidence; negative/security/privacy/accounting evidence; tests
and all ten checks; protected counts/usage/close; hook absence; and cleanup. Retain no
prohibited values.

On pass, post the hook-free implementation and report heads directly to Local Coding
PR #7 for prepared 005-n. Do not merge/auto-merge. Require all ten report-head checks,
then write exactly two response FIFO bytes `OK` once.
