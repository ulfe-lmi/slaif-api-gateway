# OAP Work Order — 155-h

PR mode: `AMEND_EXISTING_PR`
PR: `#291`
Branch: `oap/155-local-coding-signed-server-module`
Base: `main @ 7ffce834915b74809109e8b579d8541cdcfa9df7`
Current remote head: `422b666ac1d03bd9953430bb049921b3f2fb133b`

## Objective and reason

Eliminate the opaque composition-harness failure, prove the complete disposable
topology and traffic against a deterministic fake Qwen, then perform at most one
final protected-Qwen composition for real acceptance.

155-g passed exact topology, Local configuration, fixture, protected model,
repository-cleanliness, and CI gates. Its one composition then returned only
`unexpected_composition`, because the verifier localizes the entire topology and
traffic path as one outer stage. It cleaned successfully but established no
product fact. Repeating that opaque verifier would be invalid.

This continuation changes only evidence tooling: fixed per-operation stage
localization plus a full fake-Qwen rehearsal. It does not change Gateway or
Local Coding product behavior or relax the real acceptance contract.

## Verified starting state

- Gateway PR #291 is OPEN, non-draft, MERGEABLE/CLEAN at immutable 155-g
  report head `422b666ac1d03bd9953430bb049921b3f2fb133b`; its first parent is
  implementation head `a99533ef4b1e885893eedd5dfc4a7bd45f60457d`, and it changes
  only `oap/reports/155-g-corrected-single-composed-acceptance.md`.
- All ten report-head checks are successful.
- Local Coding PR #7 remains OPEN, clean, and green at exact immutable head
  `6ee2a51aa7b03d4df46e0662d88cc33fd0ef7db8`; signed-contract head
  `356be8345dd71d6fddf829278651d18e485731d4` remains an ancestor.
- The exact Local checkout is Git-clean, ignored-state-clean, has no `.venv` or
  generated bytecode/cache, and retains its tracked `uv.lock` unchanged.
- No 155-f/g/h temp root, environment, process, listener, container, database,
  or runtime reference remains.
- No real signed-session/cache/Qwen/accounting acceptance exists yet.

## 1. Fixed safe composition substages

Replace the outer-only `unexpected_composition` boundary with explicit bounded
stages covering at least:

```text
postgres_image
postgres_start
migration
database_seed
relay_start
failure_provider_start
qwen_relay_start
local_start
gateway_start
qwen_relay_ready
local_health
local_readiness
gateway_health
gateway_models
ordinary_response
stream_response
image_response
constitution_root_first
constitution_root_reuse
zero_root_rehydration
codex_session_a
codex_session_a_resume
codex_session_b
replay_and_tamper
second_gateway_key
preprovider_negatives
controlled_provider_failure
accounting
local_metrics
qwen_wire_evidence
privacy
process_cleanup
container_cleanup
repository_cleanup
protected_postcheck
final_evidence
```

- Preserve existing named `VerificationError` codes.
- Convert any other exception to `unexpected_<exact-stage>` with no exception
  message, representation, traceback, body, path, endpoint, identity, or secret.
- For official-client HTTP failures retain only fixed stage and numeric status/
  fixed public error code when independently allowlisted; never retain response
  content.
- Capture subprocess exit only as fixed process/stage/zero-or-nonzero facts.
  Task logs may exist mode 0600 only during diagnosis and must be scanned and
  deleted; never include raw log text in Git/OAP evidence.
- Unit-test every stage name, unexpected localization, error sanitization, and
  cleanup precedence. Unknown/unset stage fails closed.

## 2. Complete fake-Qwen full-stack rehearsal

Add a verifier mode that runs the same exact Gateway/Local/relay/PostgreSQL/
Codex composition against a task-owned deterministic fake Qwen instead of the
protected endpoint.

The fake must be a narrow wire double, not a synthetic Gateway envelope:

- exact `/health`, `/v1/models`, `/v1/responses`, and compiler API required by
  the pinned Local implementation;
- valid fixed non-streaming Responses, incremental SSE with a deliberately held
  first event, typed `response.completed`, and fixed nonzero usage;
- valid strict compiler response matching the pinned Local compiled-index schema
  so cache/rehydration traffic is exercised, not disabled;
- one-image request acceptance without decoding or persisting the image;
- real exact Codex 0.149.0 A1/explicit-resume-A2/B processes through the actual
  Gateway and Local apps;
- Qwen-relay safe wire facts proving credential replacement, call counts,
  compiler versus inference calls, no disabled search tools, and no internal
  headers/body markers;
- all identity/replay/tamper/key/quota/search-choice/failure/accounting/cache/
  rehydration/privacy and cleanup assertions from the real verifier.

Use the same PostgreSQL migration/seeding, Gateway/Local processes, signed
transport, official clients, relays, and evidence assertions as real mode.
Only the terminal Qwen implementation and protected credential/reference differ.
Do not loosen production assertions for fake mode; mode-specific facts must be
explicit and fail closed.

The fake rehearsal may be corrected and rerun while it exposes harness/test-
double defects, because it performs no protected inference. If it exposes a
Gateway/Local product-contract failure, stop and report it; do not mask it or
proceed to protected Qwen.

## 3. Pre-real gate

Protected execution is forbidden until all of the following pass on one clean
implementation head:

- focused verifier/stage/fake-relay tests, Ruff, compile, diff, docs, and affected
  exact Codex/client/identity/tool-policy/accounting tests;
- exact 155-g report and 155-h activation topology, current local/remote PR head,
  Local pin/ancestry, and all ten required Gateway checks;
- actual Local `load_settings` in a task environment with bytecode disabled;
- exact no-provider official Codex 0.149.0 relationship capture;
- one complete fake-Qwen composed rehearsal with fixed PASS and every evidence
  assertion, followed by complete cleanup;
- both repositories clean including ignored task state, no task `.venv`, no
  untracked lock, no task process/listener/container/temp root;
- mode-0600 `/tmp/slaif-155f-runtime.env` shape and scrubbed protected
  health/model identity, without printing or retaining values.

Commit/push all verifier/test work and require all ten checks green again after
the fake rehearsal before protected execution.

## 4. One protected-Qwen attempt

After the pre-real gate passes, invoke real mode exactly once. It must prove the
complete 155-f/155-g acceptance matrix:

- real Codex 0.149 A/resume/B through exact Gateway and Local heads;
- ordinary, incremental streaming, image, signed session stability/isolation,
  second-key isolation, cache reuse, zero-root rehydration;
- replay/tamper/preprovider negatives and controlled separate-provider rollback;
- scrubbed Qwen wire facts and real nonzero compiler/inference/provider calls;
- finalized PostgreSQL usage/accounting, zero pending state, empty external-tool
  facts, privacy, and full cleanup;
- protected model health/identity unchanged after cleanup.

No retry after protected inference or any real-mode Docker/listener startup. On
failure publish the exact fixed stage/code and precise ran/not-run ledger, with
no product conclusion beyond evidence actually reached.

## Acceptance and anti-false-positive law

PASS requires both:

1. `FAKE_REHEARSAL=OK` with the full assertion matrix; and
2. `RESULT=OK status=real_composed_acceptance` from one protected attempt.

The terminal strings alone are insufficient: report the safe stage/count/status
ledger and independently verify topology, accounting, wire facts, and cleanup.
Mocks/unit/CI/fake success cannot replace protected success. Protected success
cannot excuse a fake-mode assertion difference. Never fabricate boolean facts,
set evidence flags without their observed checks, or infer Qwen wire state from
Gateway-to-Local traffic.

## Exact allowed paths

```text
scripts/verify_local_coding_full_stack.py
tests/unit/test_local_coding_full_stack_verifier.py
docs/module-architecture.md
docs/provider-forwarding-contract.md
docs/responses-compatibility.md
docs/security-model.md
docs/accounting.md
docs/compatibility-matrix.md
oap/orders/155-h-staged-fake-rehearsal-and-final-real-acceptance.md
oap/reports/155-h-staged-fake-rehearsal-and-final-real-acceptance.md
oap/active
```

No Gateway/Local product module, schema, migration, dependency, lockfile,
route/pair/header, Compose, deployment, external repository, protected service,
release, or production mutation is authorized. Reuse current dependencies and
existing test helpers where possible; do not vendor Local implementation.

## Cleanup, publication, and merge gate

- All fake and real paths own exact unique roots/names and clean in `finally`.
  Remove only task-owned environments/config/cache/logs/Codex state/relays/
  processes/listeners/PostgreSQL state/newly pulled image/runtime reference and
  generated bytecode. Preserve tracked Local `uv.lock` and all unrelated state.
- Keep both integration PRs open; coding agent never merges or enables auto-merge.
- Push all non-report changes and require all ten checks green. Publish exactly
  one immutable `oap/reports/155-h-staged-fake-rehearsal-and-final-real-acceptance.md`
  with literal implementation head and `Report publication commit: SELF`; its
  parent is the implementation head and it changes only that report.
- After report publication make no mutation; verify remote head/topology/checks,
  send exact response FIFO `OK`, and end the round.
- A passing report creates the exact tested merge pair. Strategic then merges
  Local PR #7 first, verifies tested/signed-contract ancestry, rechecks unchanged
  Gateway head/checks/reviews, and merges PR #291 second. Do not activate
  Objective 156 or Local Objective 006 before this gate resolves.
