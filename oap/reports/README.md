# OAP execution reports

The coding agent publishes exactly one immutable `NNN-L-*.md` report for each
active execution round. The strategic model reads and independently verifies
reports but never edits them.
